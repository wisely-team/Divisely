# MongoDB Atlas Connection String
# Get this from your MongoDB Atlas dashboard: https://cloud.mongodb.com
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGODB_URI=

# JWT Secret for authentication (generate a secure random string)
JWT_SECRET=your_jwt_secret_here
JWT_REFRESH_SECRET=your_refresh_secret_here

# Server Port (Cloud will handle backend port in production for us!)
PORT=8080

# Frontend Origin - Allowed origins for CORS
# You can add multiple origins separated by commas 


# For local development, use localhost URLs
# Production URLs will be configured in cloud dashboard
FRONTEND_ORIGIN=http://localhost:3000,http://localhost:5173 # wisely.tr , www.wisely.tr


# Node Environment
NODE_ENV=production

# Email Configuration (Brevo HTTP API)
# IMPORTANT: Use the HTTP API key, NOT the SMTP key!
# Get from: Brevo Dashboard → Settings → API Keys
# The key should start with "xkeysib-"
BREVO_API_KEY=xkeysib-xxxxxxxx-xxxx

# Optional: Custom sender email (must be verified in Brevo)
# BREVO_SENDER_EMAIL=noreply@yourdomain.com
# BREVO_SENDER_NAME=YourAppName

# Google Gemini API Key (for AI features)
# Get from: https://aistudio.google.com/app/apikey
# The key should start with "AIza"
GEMINI_API_KEY=AIza-your-key-here
